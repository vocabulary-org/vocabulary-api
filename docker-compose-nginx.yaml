services:
  nginx:
    image: nginx:alpine
    container_name: vocabulary-nginx
    ports:
      - "80:80"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
    restart: unless-stopped
    networks:
      - vocabulary-network

networks:
  vocabulary-network: {}   # no 'external'
