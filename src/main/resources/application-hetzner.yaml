spring:
  datasource:
    url: jdbc:postgresql://vocabulary-db:5432/vocabulary_api
    username: enrico
    password: enrico

  security:
    oauth2:
      resourceserver:
        jwt:
          #issuer-uri:  https://20.223.169.151/realms/vocabulary
          issuer-uri:  ${SB_SECURITY_ISSUER_URI}
          jwk-set-uri: ${SB_SECURITY_JWK_SET_URI}

application:
  spa:
    url: ${APPLICATION_SPA_URL}
  keycloak:
    url: ${APPLICATION_KC_URL}
    admin-client-secret: ${KEYCLOAK_ADMIN_CLIENT_SECRET:changeme}

  cors:
    #allowed-origins: http://localhost:4200,https://gentle-tree-062b47803.1.azurestaticapps.net
    allowed-origins: ${APPLICATION_CORS_ALLOWED_ORIGINS}



